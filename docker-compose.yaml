version: '2.2'

services:
  api:
    image: back-app2
    build: .
    ports:
      - "3006:3006"
    restart: on-failure
    container_name: back-app2
    command: npm run start
    volumes:
      - .:/app
      - /app/node_modules
